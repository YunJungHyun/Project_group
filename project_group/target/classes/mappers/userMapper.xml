<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 실질적인 쿼리문을 작성하는 곳 -->
<mapper namespace="com.spring.mapper.userMapper">

	<select id="idCheck" resultType="int">
		 <![CDATA[
		SELECT COUNT(*) FROM user WHERE userid = #{userid}
		]]>
	</select>
	
	
	<insert id="signUp">
		 <![CDATA[
			INSERT INTO user(userid, userpw, username, nickname, birth ,gender, phn, email   ) VALUES(#{userid},#{userpw},#{username},#{nickname},#{birth},#{gender},#{phn},#{email})
		]]>
	
	</insert>
	
	<insert id="kakaoReg">
		 <![CDATA[
			INSERT INTO user(usercode,userid, nickname, gender, profile_img ,thumnail_img ,loginsort  ) VALUES(#{usercode},#{userid}, #{nickname}, #{gender}, #{profile_img},#{thumnail_img}, #{loginsort})
		]]>
	
	</insert>
	
	<insert id="googleReg">
		 <![CDATA[
			INSERT INTO user(usercode,userid,username,loginsort  ) VALUES(#{usercode},#{userid}, #{username}, #{loginsort})
		]]>
	
	</insert>
	
	<select id="loginPwChk" resultType="int">
		
		 <![CDATA[
			SELECT COUNT(*) FROM user WHERE userid = #{userid} AND userpw = #{userpw}
		]]>
		
	</select>


	<select id="getUserInfo" resultType="userVO">
		
		 <![CDATA[
			SELECT * FROM user WHERE userid = #{userid} AND userpw = #{userpw}
		]]>
		
	</select>
	
	
</mapper>

 